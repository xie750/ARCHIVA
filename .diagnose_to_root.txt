Please fix BuildingViewer local GLB camera transform. GLB is valid (HTTP 200 457052B and GLTFLoader.parse succeeds). Current code model.position.sub(center) then scale then model.position.y -= halfHeight places model bounds y~-3.32..-1.03 while camera target y=2.2, so object is below view. Use scaled center: model.scale.setScalar(scale); model.position.set(-center.x*scale, -center.y*scale + (size.y*scale)/2 + 0.2, -center.z*scale); controls.target.set(0,1.3,0). Add console.error in loader error callback.
